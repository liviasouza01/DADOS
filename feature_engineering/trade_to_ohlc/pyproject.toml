[project]
name = "src"
version = "0.1.0"
description = ""
authors = [
    {name = "Livia Souza",email = "liviasouza01@users.noreply.github.com"}
]
readme = "README.md"
requires-python = ">=3.12,<4"
dependencies = [
    "quixstreams (>=3.25.0,<4.0.0)"
]

[tool.poetry]
packages = [{include = "src", from = "src"}]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
